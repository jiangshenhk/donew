name: Update Market Price Cache

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  update-price:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update price cache
        run: |
          echo "Price cache update placeholder"
          echo "Future step will call market data updater"
