# AI综合卖Put决策

## 工具定位

整合新闻摘要、市场行情、K线相似度与技术分析、期权温度及具体合约门槛。三个分析模块可并行执行，最终由统一规则引擎裁决并生成完整卖Put决策报告。

## 入口

- 页面：`sell-put-decision-tool.html`
- VPS 页面镜像：`kline_robot_vercel/sell-put-decision-tool.html`
- 生产 API：`vps-backend/src/api/sell-put-decision.js`

## 与现有工具的关系

这不是替换现有四个工具，而是在它们之上新增的**聚合决策层**：

```
                       ┌─ 新闻 (jin10news 缓存)
                       ├─ 行情 (stockprice 缓存)
sell-put-decision ────┼─ K线形态 (Yahoo Finance)
                       ├─ 期权概览 (Barchart overview)
                       ├─ 期权链自动匹配 (Barchart options/get, 共用 `_lib/barchart-options-chain.js`)
                       └─ 三个分析模块 + 统一规则裁决 → 一份决策报告
```

## 处理流程

```
用户输入标的
  → 点击"自动获取期权参数"
  → 前端调用 /api/barchart-overview 填入期权概览（IV/HV 等）
  → 合约字段（行权价/delta/bid/ask/到期日）可留空
  → 点击"生成综合卖Put决策"
  → API 自动从 Barchart 期权链拉取 Delta≈0.15、最近到期(≥5天)的 Put 合约
  → prepare 阶段加载：
     1. stockprice/data/latest-price.json（行情快照）
     2. jin10news/data/latest-24h.json（新闻缓存）
     3. 期权温度与新闻事件预分析
  → 前端并行请求三个模块：
     1. 市场与黑天鹅风险
     2. K线相似度、历史条件样本、ABC/2B结构、ATR与支撑阻力
     3. 期权温度与具体合约
  → finalize 阶段使用K线模块实际ATR重新计算合约硬门槛
  → 执行关键数据完整性检查；不完整时只输出预检查，不伪造完整结论
  → 统一规则引擎裁决，AI只能补充解释或收紧结论，不能绕过硬门槛
  → 返回固定六节 HTML 报告
```

> 合约字段**支持自动获取**：留空时 API 从 Barchart 期权链自动匹配。手工输入仍可覆盖自动匹配结果。

## 报告内容契约

报告格式的唯一详细规范位于 [`设计_综合卖Put决策.md`](./设计_综合卖Put决策.md)。修改提示词、模块字段或 HTML 模板前必须先阅读该文件。

以下内容属于不可简化的验收条件：

| 章节 | 必须回答的内容 | 最低详细度 |
|---|---|---|
| 综合结论 | 固定三档结论；恐慌溢价、大跌/跳空风险、权利金是否值得三个问题 | 每个问题必须有独立答案、具体数字和因果理由 |
| 市场环境 | 科技/半导体、VIX、利率/美元、跨资产、综合共振 | 4-5条带行情数字的完整论证 |
| 期权温度 | IV/HV、IV Rank/Percentile、两类PCR、Expected Move、恐慌溢价 | 5条定价解释，不能只罗列指标 |
| K线技术 | 趋势、典型形态、支撑阻力、ATR安全垫、量价 | 5条技术解释并引用具体数值与匹配度 |
| 综合建议 | 合约门槛、关键风险、必要操作条件、行权价区间、特别提醒 | 风险必须包含触发条件和后果 |
| 未来3-5日 | 具体监控项、好转信号、恶化信号、已验证事件 | 必须给出可观察的价格或指标条件 |

历史内容基准保存在 [`examples/`](./examples/)：`INTC-sell-put-decision_20260730_184516.html` 与 `IBIT-sell-put-decision_20260730_184259.html`。历史文件不进入生产依赖，但其分析密度是回归验收基准。

禁止的退化形式：

- 只保留六个标题，每节只有一句摘要。
- 用“偏多、偏空、高风险”等标签替代数字和因果解释。
- 把缺失数据当作 `0`，或根据训练记忆补造行情、事件与日期。
- AI 输出覆盖代码层风险底线或具体合约硬门槛。
- 任一模块失败后把整份报告缩减成行情摘要。

## 约定

- API 并行加载行情和新闻（`Promise.all`），避免串行等待
- K线数据取最近3个月日线，计算技术指标（SMA5/10/20/50、ATR、量比、形态检测）
- 合约字段缺失时，自动调用 `_lib/barchart-options-chain.js` 从 Barchart 拉取匹配合约
- 截图 OCR 在浏览器本地执行（可选）；API 不读取图片、不调用视觉模型
- 三个分析模块分别调用 DeepSeek；单模块失败时只将该模块降级为规则版
- K 线结构分析最多等待 30 秒；超时后进入完整性预检查
- 规则版不是备用空壳：AI不可用时仍使用市场风险、期权温度、K线结构、新闻事件和具体合约门槛
- 最终裁决重新使用真实 ATR、DTE、Delta、Bid/Ask 和 Expected Range，不依赖模块完成顺序
- 报告固定包含：综合结论、市场环境、期权温度、K线技术信号、综合建议、未来关注清单，以及折叠的数据来源与时间
- 三个模块必须输出带具体数字的详细论证：市场5类因果链、期权5项定价解释、K线5项技术解释；综合节必须回答恐慌溢价、大跌/跳空风险、权利金是否值得三个问题
- 综合建议包含关键风险、必要操作条件、安全/边缘/危险行权价区间及特别提醒；关注清单包含明确的好转与恶化信号
- API 返回模块状态、警告和总耗时，前端展示模块进度
- 三个耗时模块以后台任务方式启动，接口立即返回；前端每2秒轮询一次状态，最长等待2分钟，避免Nginx或浏览器长连接中断导致 `Failed to fetch`
- prepare、任务启动、状态查询和 finalize 对网络中断及 502/503/504 做有限重试；同一 `reportId` 的模块结果可安全复用
- VPS AI 路由只执行一次来源校验与请求计数，避免路由层和Handler重复计数

## 版本

v2.1.3｜2026-08-06 16:10｜后台任务轮询并修复偶发Fetch失败
